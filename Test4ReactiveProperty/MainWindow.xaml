<Window x:Class="Test4ReactiveProperty.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Test4ReactiveProperty"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" MouseMove="Window_MouseMove">
    <Grid>
        <Canvas>
            <Ellipse Fill="Blue" Width="10" Height="10" Canvas.Left="{Binding Points[0].Value.X}" Canvas.Top="{Binding Points[0].Value.Y}"/>
            <Ellipse Fill="Red" Width="10" Height="10" Canvas.Left="{Binding Points[1].Value.X}" Canvas.Top="{Binding Points[1].Value.Y}"/>
            <Ellipse Fill="Green" Width="30" Height="30" Canvas.Left="{Binding Points[3].Value.X}" Canvas.Top="{Binding Points[3].Value.Y}"/>
            <Ellipse Fill="GreenYellow" Width="20" Height="20" Canvas.Left="{Binding Points[2].Value.X}" Canvas.Top="{Binding Points[2].Value.Y}"/>
        </Canvas>
    </Grid>
</Window>
